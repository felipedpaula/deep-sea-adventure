# src/ui

Componentes de interface: HUD, menus, telas auxiliares e informacoes de progresso.

- `dom.js` — centraliza as referencias aos elementos do DOM (canvas, hud, menus, app).
- `hud.js` — atualiza o HUD a cada quadro (marcador de profundidade no canto inferior esquerdo).
